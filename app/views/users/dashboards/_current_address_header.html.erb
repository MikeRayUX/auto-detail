<% if user.completed_setup? %>
  <div class="w-full border-b flex justify-center items-center bg-white py-2">
    <%= link_to user.address.truncated_street_address(23), 
      users_dashboards_settings_update_addresses_path,
      class: 'text-base font-black text-primary sm:text-xl'%>
  </div>
<% end %>